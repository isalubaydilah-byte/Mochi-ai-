# Mochi AI 2.0
Backend Node/Express + OpenAI Responses API + web research + Android client.
Deploy root backend to Render, set OPENAI_API_KEY, then put the HTTPS backend URL in Android Api.kt. Never put the API key in Android or GitHub.
